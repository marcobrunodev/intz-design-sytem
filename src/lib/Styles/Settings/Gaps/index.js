import { createGlobalStyle } from 'styled-components'

const Gaps = createGlobalStyle`
:root {
  --gap-smallest: 1rem;
  --gap-small: 1.5rem;
  --gap-medium: 2rem;
  --gap-big: 3rem;
  --gap-bigest: 4rem;
}
`
export default Gaps
